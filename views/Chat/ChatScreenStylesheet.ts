import {StyleSheet} from "react-native";

export const CHAT_SCREEN_STYLES = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#fff',
    },
    chatContainer: {
        flexGrow: 1,
        justifyContent: 'flex-end',
        paddingVertical: 16,
    },
});